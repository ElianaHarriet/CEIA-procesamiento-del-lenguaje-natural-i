[project]
name = "ceia-procesamiento-del-lenguaje-natural-i"
version = "0.1.0"
description = "Repositorio del curso Procesamiento del Lenguaje Natural I (CEIA)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.7",
    "scikit-learn>=1.7.2",
]

[project.optional-dependencies]
dev = [
    "jupyter>=1.0.0",
    "ipykernel>=6.29.0",
]
